# test-client-design-system — AI Coding Instructions
# Generated by Guing v0.0.3 on 2026-04-17T10:36:31.580Z
# Base UI library: Material UI (mui)

## Package import
Always import from `test-client-design-system` — NEVER from the underlying library directly:
```tsx
import { Button, vars } from 'test-client-design-system';
import 'test-client-design-system/src/design-tokens/tokens.css';
```

## Design Token Rules
- NEVER hardcode hex colors — use CSS variables or `vars` references
- NEVER hardcode font sizes or spacing values
- TypeScript access: `import { designTokens, vars } from 'test-client-design-system'`

## vars.* shape (typed CSS-variable references)
- vars.color           — greyPage, greyPageLight, greyButtonsBg, greyTextField, greyStroke, greyDrawerHandle, greyTextIcons, orangeOrangeWarning, greyLightGrey, turquoiseTurquoise, greySecondaryBodyText, whiteWhite, … (33 total)
- vars.radius          — small, medium, large, round
- vars.radiusToken     — radiusXXS, radiusXS, radiusS, radiusM, radiusL, radiusXL, radiusFull, small … (11)
- vars.font            — labelSml, headlineMed18Med, headlineMedHeadlineReg16Reg, headlineMedHeadlineReg16Med, headlineMedHeadlineRegLabelLarge14Med, labelReg14Reg, lableSami12, heading1 (each: .family .size .weight .lineHeight)
- vars.spacing         — spaceXXS, spaceXS, spaceS, spaceM, spaceL, spaceXL, spaceXXL, spaceXXXL
- vars.size            — borderWidthXXS, borderWidthXS, borderWidthS, borderWidthM, borderWidthL, opacityHover, opacityFocus, opacityPressed
- vars.gradient        — mILogoGradient, brandGradient, brandPurple

Usage example:
```tsx
import { vars } from 'test-client-design-system';
<Box sx={{ backgroundColor: vars.color.actionPrimary, borderRadius: vars.radius.medium }} />
```

## Namespace Component API
All Figma-derived components are exported as namespace objects.
Import and use them like this:
```tsx
import { Blurs, Borders, Colors, Elevations, Gaps, Grids, Icons, Injection, Model, Opacity, Radius, Shadows, Sizes, Text, Token, Typography, Welcome } from 'test-client-design-system';

<Blurs.default onClick={fn} isLoading={false} />
<Borders.default onClick={fn} isLoading={false} />
<Colors.default onClick={fn} isLoading={false} />
<Elevations.default onClick={fn} isLoading={false} />
<Gaps.default onClick={fn} isLoading={false} />
<Grids.default onClick={fn} isLoading={false} />
<Icons.default onClick={fn} isLoading={false} />
<Injection.View onClick={fn} isLoading={false} />
<Model.Map onClick={fn} isLoading={false} />
<Opacity.default onClick={fn} isLoading={false} />
<Radius.default onClick={fn} isLoading={false} />
<Shadows.default onClick={fn} isLoading={false} />
<Sizes.default onClick={fn} isLoading={false} />
<Text.default onClick={fn} isLoading={false} />
<Token.MapView onClick={fn} isLoading={false} />
<Typography.default onClick={fn} isLoading={false} />
<Welcome.default onClick={fn} isLoading={false} />
```

### Standard props (every AI-generated component accepts these)
- `onClick?: () => void` — click handler
- `onHover?: () => void` — mouse-enter handler
- `isDisabled?: boolean` — disables the component
- `isLoading?: boolean` — shows a spinner; disables interaction
- `children?: React.ReactNode` — content slot
- `sx?: Record<string, unknown>` — style overrides (spread last; developer wins)
- `className?: string` — class overrides

## Available Components (17 total)

### Blurs
  Blurs component
  Props:
    (no props)

### Borders
  Borders component
  Props:
    (no props)

### Colors
  Colors component
  Props:
    (no props)

### Elevations
  Elevations component
  Props:
    (no props)

### Gaps
  Gaps component
  Props:
    (no props)

### Grids
  Grids component
  Props:
    (no props)

### Icons
  Icons component
  Props:
    (no props)

### InjectionView
  InjectionView component
  Props:
    - libraryName: string (required)
    - approach: string (required)
    - bindings: TokenBinding[] (required)

### ModelMap
  ModelMap component
  Props:
    (no props)

### Opacity
  Opacity component
  Props:
    (no props)

### Radius
  Radius component
  Props:
    (no props)

### Shadows
  Shadows component
  Props:
    (no props)

### Sizes
  Sizes component
  Props:
    (no props)

### Text
  Text component
  Props:
    (no props)

### TokenMapView
  TokenMapView component
  Props:
    (no props)

### Typography
  Typography component
  Props:
    (no props)

### Welcome
  Welcome component
  Props:
    (no props)

## Token Summary
- Colors: 33 tokens
- Typography: 14 tokens
- Radius: 11 tokens
- Gaps: 8 tokens
- Sizes: 25 tokens
- Gradients: 3 tokens

## Component Conventions
- All components use `React.forwardRef`
- Developer overrides always win via `...sx` or `...style` spread
- Do NOT import from `@mui/material` directly — use DS package re-exports
